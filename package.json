{
  "name": "rzpx-fav-demo",
  "version": "1.0.0",
  "description": "RazorpayX Fund Account Validation — Lending Firm Onboarding Demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"nodemon server.js\" \"browser-sync start --proxy localhost:3000 --files 'public/**' --no-open --no-notify\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "nodemon": "^3.0.2"
  }
}
